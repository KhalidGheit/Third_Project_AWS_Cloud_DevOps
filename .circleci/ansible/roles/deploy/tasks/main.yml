
- name: "extract the zipped artifact artifact.tar.gz into the EC2 instance"
  become: yes
  shell: |
    tar -xvf artifact.tar.gz
    cd backend
    npm install
    pm2 stop default
    pm2 start npm -- start